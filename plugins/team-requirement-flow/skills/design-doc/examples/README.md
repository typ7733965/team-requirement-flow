# 设计方案示例

本目录是 `design-doc` Skill 的**虚构示例产出**——一个「短链接服务」的完整设计方案。

> 示例与任何真实项目无关，仅用于演示 Skill 应输出的文档结构与文风。

## 文件

- `短链接服务设计方案.md` —— 完整 9 节设计方案

## 阅读要点

1. **第二节 整体架构** —— ASCII 时序图 + 组件表 + 调用链路
2. **第三节 数据模型** —— MySQL 表结构 + Redis Key 命名规则
3. **第五节 错误码** —— 字符串字面量（如 `SL_NOT_FOUND`）+ 触发条件 + 前端处理建议
4. **第六节 关键设计决策** —— 候选 → 选择 → 理由
5. **第七节 实施步骤拆分** —— 每步可独立 commit

## 注意

文档中所有接口、字段、错误码均为**虚构**，请勿当真实代码参考。团队成员可以基于此对齐写设计方案的"调性"。
